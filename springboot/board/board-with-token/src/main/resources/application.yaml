spring:
  application:
    name: board-with-token
  datasource:
    url: jdbc:mysql://localhost:3306/board_token?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 6731
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

file:
  upload-dir: ./uploads
